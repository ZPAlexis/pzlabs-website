//const homeButtonIcon = container.querySelector('x');
//const homeButtonText = container.querySelector('x');

/*export function mobileNavSelect(button) {
  const target = button.dataset.target;
  // if target = '/' -> change home button styling
  // if target = '/projects.html' -> change projects button styling
  // if target = '/about.html' -> change about button styling
  // revert original active button styling
}*/